# Changelog

## v1.0.21 - 2026-08-05

- 自定义主题背景改为优先应用于书架主界面，并移入全局“设置 → 外观”；正文背景保留为独立开关且默认关闭，避免复杂图片影响阅读可读性。
- 拆分超大的阅读器代码：`ReaderView` 的样式与纯展示逻辑独立成模块，reader store 的配置迁移、主题模型和背景同步控制器分别维护，降低后续功能修改的耦合与回归范围。
- 背景同步协议增加独立的正文启用字段；旧服务端背景元数据仍可直接读取，不需要数据库或文件迁移。

## v1.0.20 - 2026-08-05

- 阅读主题支持上传自定义背景图，并在登录设备间同步；提供填充方式、对齐位置和遮罩强度设置，同时支持离线缓存与失败重试。
- 服务端新增阅读背景 API 与独立文件存储，限制单图 15 MB，并校验 JPEG、PNG、WebP、AVIF 的真实文件签名；上传、设置更新和删除操作增加并发保护。
- 阅读进度增加修订号与服务端冲突保护，旧设备的滞后保存不会再覆盖较新的阅读位置，同时保留旧客户端兼容性。
- 听书新增音量滑块、媒体会话与熄屏续播支持、自定义 1–1440 分钟定时停止及无效输入提示。
- 优化跨页听书分段：同一自然段跨页时保持为一个语音请求；OpenAI/Azure 播放增加预取与异步竞态清理，减少段落间停顿和旧音频回跳。
- 浏览器章节缓存增加 200 MiB / 30 天 LRU 治理；服务端章节缓存默认限制为 1 GiB / 30 天，并可通过 `CACHE_MAX_BYTES`、`CACHE_MAX_AGE_DAYS` 调整。
- 开发与测试构建改用较小的调试信息配置，降低本地 `target` 目录持续膨胀的速度；README 与环境变量示例同步更新。
- 本次更新新增后端 API 与文件存储目录，但不需要数据库迁移；部署时应确保 `storage` 持久化且可写。

## v1.0.19 - 2026-07-30

- 修复听书启动位置：从当前阅读页顶部可见段落开始，不再在左右翻页模式下回到章节开头。
- 听书推进到下一页内容时会同步切换横向页面；OpenAI/Azure 合并请求按页面截断，避免声音位置与页面不同步。
- 选中文本悬浮菜单新增“从本段听”，并将净化入口明确为“本书净化”和“书源净化”；跨多段选择时从选区覆盖的第一段开始。
- 跨设备恢复进度增加书籍身份校验：仅当服务端返回的 `bookUrl` 与本地会话一致时覆盖进度，防止启动时跳到另一部书。
- 修复 OpenAI/Azure 分段听书错误复用旧屏幕位置、导致同一段反复播放的问题；手动切换段落现在会覆盖待执行的自动切段。
- 选区操作菜单改为默认启用，并为旧浏览器配置执行一次性迁移；用户之后仍可在阅读设置中切换为“忽略”。
- 本次更新仅涉及前端阅读器与会话恢复逻辑，不包含后端 API、数据库结构或存储格式变更。

## v1.0.12 - 2026-07-01

- 书源登录预览支持脚本型 `loginUrl`：后端会从 `function login()`、`java.startBrowserAwait(...)` 或 `const/let/var url = ...` 中提取真实登录入口，不再只把 App 专用 JS 当作普通 URL 请求。
- 书源登录预览支持内置表单页：当书源提供邮箱/密码式 `loginUi` 时，后端生成可预览的 HTML 表单，前端用 iframe `srcdoc` 展示，方便直接测试登录接口返回。
- 书源登录解析补了覆盖用例：包含 `source.bookSourceUrl` 拼接、忽略 `login()` 外部 helper 调用、以及没有 `startBrowserAwait` 时从 `url` 赋值里提取登录地址。
- 书源管理页改为复用全局书源 store：打开管理页时不重复维护一份本地 sources/loading 状态，避免书源列表和其他页面状态不一致。
- 搜索结果增加 30 分钟内存缓存：同一关键词 + 搜索范围 + 分组/书源再次搜索时直接复用缓存结果，减少重复请求。
- 搜索范围偏好会持久化：用户选择“全部 / 分组 / 当前书源”后会保存，下次打开搜索继续沿用；从探索页发起搜索时只在有当前书源时限定 source scope。
- 搜索结果页在切换范围、分组、书源时会同步保存偏好，并在搜索完成后缓存本次结果。
- 阅读设置迁移更稳：字体、行高、段距、页面宽度、滚动速度、AI 面板宽度/字号等数值会做 finite/min 校验，旧配置或坏数据不会把设置写成非法值。
- 朗读设置迁移更稳：provider、OpenAI source、音频格式、请求模式和语速/音调/停止时间会回退到合法默认值。
- 设置抽屉把“服务端版本更新”和“浏览器前端缓存更新”拆开显示：服务端更新提示为“需手动部署”，前端缓存更新只负责刷新浏览器资源，不再让这两个概念混在一起。
